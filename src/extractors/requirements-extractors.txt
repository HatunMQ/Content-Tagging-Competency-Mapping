# Core extractors (data extraction step)
python-pptx>=0.6.23
openpyxl>=3.1.0
 
# Optional OCR for pptx slides where text lives inside an image
# (the code degrades gracefully if these aren't installed — OCR just gets skipped)
pytesseract>=0.3.10
Pillow>=10.0.0
# also requires the system Tesseract binary, e.g.:
#   Ubuntu/Debian: sudo apt-get install -y tesseract-ocr tesseract-ocr-ara
#   macOS:         brew install tesseract tesseract-lang